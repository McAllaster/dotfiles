# See `man 5 sway` for a complete reference.

#   ad88888ba
#  d8"     "8b
#  Y8,
#  `Y8aaaaa,    8b      db      d8  ,adPPYYba,  8b       d8
#    `"""""8b,  `8b    d88b    d8'  ""     `Y8  `8b     d8'
#          `8b   `8b  d8'`8b  d8'   ,adPPPPP88   `8b   d8'
#  Y8a     a8P    `8bd8'  `8bd8'    88,    ,88    `8b,d8'
#   "Y88888P"       YP      YP      `"8bbdP"Y8      Y88'
#                                                   d8'
#                                                  d8'

### Variables

set $left h
set $down j
set $up k
set $right l
set $mod Mod4

set $term alacritty
set $menu wofi --show drun -i
set $lock swaylock
set $screenshot grim
set $screenshot-select grim -g "$(slurp)"

### Output
set $laptop "Unknown 0x095F 0x00000000"
set $desktop "Goldstar Company Ltd 27GN950 008NTZN5H213"

output * bg "#383a42" solid_color

output $laptop mode 2256x1504@60Hz
output $laptop scale 1.5
# Disable laptop when closed, re-enable when opened
bindswitch --reload --locked lid:on output $laptop disable
bindswitch --reload --locked lid:off output $laptop enable

output $desktop mode 3840x2160@60Hz
output $desktop scale 1.5

### Input

# Laptop Trackpad
input "2362:628:PIXA3854:00_093A:0274_Touchpad" {
  accel_profile adaptive
  click_method clickfinger
  scroll_method two_finger
  natural_scroll enabled
  scroll_factor 0.4
  tap disabled
}

# Laptop Keyboard
input "1:1:AT_Translated_Set_2_keyboard" {
  # See /usr/share/X11/xkb/rules/xorg.lst for options

  # Swap "left ctrl" and "caps lock"
  # Swap "left alt" with "left win"
  xkb_options "ctrl:swapcaps,altwin:swap_lalt_lwin"
}

# Desktop2 Keyboard
input "12851:25345:Ducky_Ducky_One2_Mini_RGB" {
  # See /usr/share/X11/xkb/rules/xorg.lst for options

  # Swap "left ctrl" and "caps lock"
  # Swap "left alt" with "left win"
  xkb_options "ctrl:swapcaps,altwin:swap_lalt_lwin"
}

# Desktop Mouse
input "1241:41483:E-Signal_USB_Gaming_Mouse" {
  accel_profile flat
  pointer_accel 0
}

# Desktop2 Mouse
input "5426:113:Razer_Razer_DeathAdder_Essential_White_Edition_Keyboard" {
  accel_profile flat
  pointer_accel 0
}

input "1133:16495:Logitech_MX_Ergo" {
  accel_profile flat
  pointer_accel 0
}

input "type:keyboard" {
  repeat_delay 250
  repeat_rate 25
}

### Key bindings

bindsym $mod+Return exec $term
bindsym $mod+Shift+q kill
bindsym $mod+d exec $menu
floating_modifier $mod normal
bindsym $mod+Shift+c reload
bindsym $mod+p exec $screenshot
bindsym $mod+Shift+p exec $screenshot-select

# Movement
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# Workspaces
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

# Layout
bindsym $mod+b splith
bindsym $mod+v splitv
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+f fullscreen
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle
bindsym $mod+a focus parent

# Media
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioPause exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous
bindsym XF86AudioRaiseVolume exec --no-startup-id pamixer -i 10
bindsym XF86AudioLowerVolume exec --no-startup-id pamixer -d 10
bindsym XF86AudioMute exec --no-startup-id pamixer -t

# Brightness
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl s +10%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl s 10%-

### Modes

mode "resize" {
    bindsym $left resize shrink width 100px
    bindsym $down resize grow height 100px
    bindsym $up resize shrink height 100px
    bindsym $right resize grow width 100px
    bindsym Left resize shrink width 100px
    bindsym Down resize grow height 100px
    bindsym Up resize shrink height 100px
    bindsym Right resize grow width 100px

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

set $mode_system System: exit (e), lock (l), suspend (s), reboot (r), shutdown (Shift+s)

mode "$mode_system" {
  bindsym e exec --no-startup-id swaymsg exit, mode "default"
  bindsym l exec --no-startup-id $lock, mode "default"
  bindsym s exec --no-startup-id systemctl suspend && $lock, mode "default"
  bindsym r exec --no-startup-id systemctl reboot, mode "default"
  bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"

  bindsym Return mode "default"
  bindsym Escape mode "default"
}

bindsym $mod+Shift+e mode "$mode_system"

### Bar

bar {
  swaybar_command waybar
}

### Style

# GTK theming
exec_always {
  # gsettings set org.gnome.desktop.wm.preferences theme "Nordic"
  # gsettings set org.gnome.desktop.interface gtk-theme "Nordic"
  # gsettings set org.gnome.desktop.interface icon-theme "Adwaita"
  gsettings set org.gnome.desktop.interface cursor-theme "Vimix-white-cursors"
  # gsettings set org.gnome.desktop.interface cursor-size 64
}

# Sway-specific cursor theming
#seat seat0 xcursor_theme "Vimix-white-cursors" 64
seat seat0 xcursor_theme "Vimix-white-cursors"
seat seat0 hide_cursor 30000

# Fonts
font pango:sans-serif 10

# Colors
set $c_black      "#383a42"
set $c_red        "#e45649"
set $c_green      "#50a14f"
set $c_yellow     "#c18401"
set $c_blue       "#0184bc"
set $c_magenta    "#a626a4"
set $c_cyan       "#0997b3"
set $c_white      "#fafafa"
set $c_foreground "#383a42"
set $c_background "#fafafa"
set $c_dim        "#d4d4d4"

client.background       "$c_background"
# <Window State>        <Border>     <Bg>         <Text>          [Indicator] [Child Border]
client.focused          "$c_green"   "$c_green"   "$c_foreground" "$c_yellow" "$c_green"
client.focused_inactive "$c_dim"     "$c_dim"     "$c_foreground" "$c_yellow" "$c_dim"
client.unfocused        "$c_dim"     "$c_dim"     "$c_foreground" "$c_yellow" "$c_dim"
client.urgent           "$c_red"     "$c_red"     "$c_foreground" "$c_yellow" "$c_red"
client.placeholder      "$c_magenta" "$c_magenta" "$c_foreground" "$c_yellow" "$c_magenta"

default_border pixel 5
gaps inner 10

### Executions

# Update D-Bus environment variables
exec dbus-update-activation-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway

# Notifications
exec mako

# Audio
exec pipewire
exec pipewire-pulse

# Idling
exec swayidle -w timeout 300 'swaylock -f' timeout 600 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"' before-sleep 'swaylock -f'

### Window actions

# Automatically remove the Firefox "sharing indicator"
for_window [title="Firefox — Sharing Indicator"] floating enable, kill;
for_window [title="Picture-in-Picture"] floating enable, resize set 800 450, move position 100ppt 100ppt, move left 800 px, move up 450 px, sticky enable, no_focus

# Laptop Clamshell Identification
exec_always $HOME/.config/sway/lidstate.sh
